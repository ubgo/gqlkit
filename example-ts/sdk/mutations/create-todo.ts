// Code generated by gqlkit. DO NOT EDIT.

import { BaseBuilder, GraphQLClient } from "../builder";
import { TodoFields } from "../fields";
import type { Todo } from "../types";
import type { NewTodo } from "../inputs";

/** CreateTodoMutationBuilder builds a mutation for createTodo */
export class CreateTodoMutationBuilder<TResult = Todo> {
  private builder: BaseBuilder;

  constructor(client: GraphQLClient) {
    this.builder = new BaseBuilder(client, "mutation", "CreateTodo", "createTodo");
  }

  /** Sets the input argument */
  input(v: NewTodo): this {
    this.builder.setArg("input", v, "NewTodo!");
    return this;
  }

  /** Configures which fields to return */
  select<T extends object>(selector: (f: TodoFields) => TodoFields<T>): CreateTodoMutationBuilder<T> {
    selector(new TodoFields(this.builder.getSelection()));
    return this as any;
  }

  /** Execute runs the mutation and returns the result */
  async execute(): Promise<TResult> {
    const query = this.builder.buildQuery();
    const variables = this.builder.getVariables();

    const response = await this.builder
      .getClient()
      .execute<{ createTodo: TResult }>(query, variables);

    return response.createTodo;
  }

  /** ExecuteRaw runs the mutation and returns the raw response */
  async executeRaw(): Promise<Record<string, unknown>> {
    return await this.builder.executeRaw();
  }

  /** Internal — exposes this operation as a fragment for batch() */
  getOpFragment(alias: string) {
    return this.builder.getOpFragment(alias);
  }
}
